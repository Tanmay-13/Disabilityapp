<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.disablity.app">

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_ic"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.SplashActivity"
            android:theme="@style/SplashTheme"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.ApplicantPastApplications" />
        <activity android:name=".ui.ApplicantHome" />
        <activity android:name=".ui.RecuiterApplicants" />
        <activity android:name=".ui.RecruiterUploadJob" />
        <activity android:name=".ui.RecruiterHome" />
        <activity android:name=".ui.ApplicantJobSearchActivity" />
        <activity android:name=".ui.RegisterActivity" />
        <activity android:name=".ui.sigup.SignUpActivity" />
        <activity android:name=".ui.login.ui.login.LoginActivity" />
    </application>

</manifest>